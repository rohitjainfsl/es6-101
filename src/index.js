import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './toggleTheme/App'


const root = ReactDOM.createRoot(document.querySelector('#wrapper'));

root.render(
  <App />
);